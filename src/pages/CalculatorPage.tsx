import { useMemo, useState } from 'react'
export function CalculatorPage(){const[price,setPrice]=useState(1600000);const[equity,setEquity]=useState(400000);const[years,setYears]=useState(30);const[rate,setRate]=useState(4.8)
 const result=useMemo(()=>{const principal=Math.max(0,price-equity);const n=years*12;const r=rate/100/12;const payment=r===0?principal/n:principal*r*Math.pow(1+r,n)/(Math.pow(1+r,n)-1);return{principal,payment,total:payment*n}},[price,equity,years,rate])
 return <section className="form-card"><span className="eyebrow">סימולציה בלבד</span><h2>מחשבון משכנתא</h2><div className="form-grid"><label>מחיר הנכס<input type="number" value={price} onChange={e=>setPrice(Number(e.target.value))}/></label><label>הון עצמי<input type="number" value={equity} onChange={e=>setEquity(Number(e.target.value))}/></label><label>שנים<input type="number" value={years} onChange={e=>setYears(Number(e.target.value))}/></label><label>ריבית שנתית משוערת<input type="number" step="0.1" value={rate} onChange={e=>setRate(Number(e.target.value))}/></label></div><div className="result-panel"><span>משכנתא נדרשת</span><strong>{result.principal.toLocaleString('he-IL')} ₪</strong><span>החזר חודשי משוער</span><strong>{Math.round(result.payment).toLocaleString('he-IL')} ₪</strong><small>זהו חישוב מתמטי בסיסי ואינו תמהיל או הצעה מהבנק.</small></div></section>}
